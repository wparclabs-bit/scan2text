# Scan2Text Current State

<!-- MAINTENANCE PROTOCOL: Keep only the Baseline block + last 5 changelog entries here. When you add a new entry, move the oldest entry to second-brain/01-Agent-Memory/Archive/state-history.md. This protects the 45k token cap (AGENTS.md 3.2). -->

## Baseline
- Phase: GATE4 (Tauri Rebuild — bundle FIX75 + FIX76) — S11-GATE4-TAURI-REBUILD-DEPLOY
- Date: 2026-08-20
- Tauri shell hash: 7120B6375022E5FB692FA4DE7AED625679994981904841EEE9A1CE0A4143474F (supersedes 6B56B7310BAF98AC10753AAFCCAC8A5ED287C016468E5D6A227D3F2BD66622FE)
- Backend hash: DAEEFBCCC0A9084B3C4B05BE59628FC76BECFF50E462BC470947FA0CA437DC8D (GATE3-RETRY, up-to-date)
- UPX: NOT installed on build machine — upx=False
- pdfium.dll: present in portable dist (_internal/pypdfium2_raw/pdfium.dll, 6.88MB)
- _internal: 707 files (actual GATE3-RETRY build), all DLLs present (python312.dll, llama.dll, pdfium.dll, 4 VC++ runtime DLLs)
- Backend tests: 331 passed + 1 pre-existing failure (test_health_contract). Full gate GREEN except pre-existing.
- Frontend tests: 649 passed, 0 failures (new baseline — 37 test files; FIX75 added 2 tests)
- PRD: v1.12 source of truth in second-brain/04-Product/
- RESULT: S11-GATE4 COMPLETE. Doc-only rebuild slice: Tauri shell rebuilt from current frontend (bundles FIX75 health404-typo fix + FIX76 Rust orphan kill). Frontend gate: 649 passed, 0 failed, typecheck clean. Deployed to D:\Scan2Text\Scan2Text.exe. Boot proof: health ok, OvisOCR2 0.9B loaded (model.loaded true), worker idle, version 0.1.0. Orphan proof: Scan2Text.exe stop → taskkill /F /T kills entire Python multiprocessing tree; port 47351 clears (TimeWait only). Zero source edits.

## Recent Changelog
- **2026-08-20 (S11-GATE4-TAURI-REBUILD-DEPLOY):** COMPLETE — Doc-only rebuild slice: Tauri shell rebuilt from current frontend (bundles FIX75 health404-typo fix + FIX76 Rust orphan kill). Frontend gate: 649 passed, 0 failed, typecheck clean. Deployed to D:\Scan2Text\Scan2Text.exe. Boot proof: health ok, OvisOCR2 0.9B loaded (model.loaded true), worker idle, version 0.1.0. Orphan proof: Scan2Text.exe stop → taskkill /F /T kills entire Python multiprocessing tree; port 47351 clears (TimeWait only). Zero source edits.
- **2026-08-20 (S11-FIX76-RUST-ORPHAN-KILL):** COMPLETE — Replaced `frontend/src-tauri/src/backend_process.rs:283-287` `stop_backend()` with Windows `taskkill /F /T /PID <pid>` to kill entire Python multiprocessing process tree. Added CREATE_NO_WINDOW flag (0x08000000) to suppress console flash. Fallback: native `child.kill()` always attempted as last resort. Port-wait loop: blocks until 47351 released (up to 5s). Non-Windows: graceful fallback with warning log. cargo check: zero errors, zero warnings. Commit `6a1f07f`.
- **2026-08-20 (S11-FIX75-HEALTH404-TYPO):** COMPLETE — TDD RED→GREEN. Root cause: `frontend/src/lib/api.ts:116` called `fetch(buildApiUrl('/health'))` — missing `/api/` prefix. Backend only serves `/api/health`. Background re-poll loop (store.ts:507) calls `getHealth()` every 60s; each 404 incremented `consecutiveHealthFailures`; after 3 failures (FIX72 threshold), job falsely marked failed with `errors.backendLost` toast. Fix: one-line edit `/health` → `/api/health`. Test gap closure: added `getHealth()` test in `api.test.ts` asserting canonical `/api/health` path via mocked fetch — this test was RED before the fix, confirming the bug was undetectable by prior gates. Frontend: 647 passed, 0 failures. Typecheck clean. Zero backend/Rust changes.
- **2026-08-20 (S11-DIAG-LINGER-PROCESSES):** DIAG — CONFIRMED. Root causes: (1) **WORKER-CHILD**: Second backend process is a Python multiprocessing fork (`--multiprocessing-fork`, parent_pid=8020) spawned by the first backend — NOT a shell double-spawn. Live evidence: PID 15380 parented by PID 8020, created 2s after first backend. (2) **ORPHAN-ON-EXIT**: Both backends survive after shell kill. Live evidence: killed Scan2Text.exe (PID 20400), both PIDs 8020+15380 survived. Root cause: backend's parent PID is 9548 (phantom/dead intermediary process), NOT the Rust shell (20400). Rust shell's BackendManager.child handle disconnected from actual backend process tree. Cleanup handlers fire on a stale handle. Minimal fix: kill process tree on exit (`taskkill /F /T /PID`), capture actual PID via port query, add spawn guard. Zero source edits. Full report: `second-brain/01-Agent-Memory/Phase-11/slice-S11-DIAG-LINGER-PROCESSES.md`.
