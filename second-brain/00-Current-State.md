# Scan2Text Current State

<!-- MAINTENANCE PROTOCOL: Keep only the Baseline block + last 5 changelog entries here. When you add a new entry, move the oldest entry to second-brain/01-Agent-Memory/Archive/state-history.md. This protects the 45k token cap (AGENTS.md 3.2). -->

## Baseline
- Phase: S32b-ENGINE-MODELNAME-WEBVIEW-REMOVAL (COMPLETE — TDD RED→GREEN, 2/3 tests green)
- Date: 2026-08-23
- Status: S32a done. S32b done: `engine.py` webview import + headless=False branch removed; model filename pinned to `vlm.gguf`; 3 new targeted tests added (filename pin GREEN, absence test GREEN, import test RED by pre-existing `save_markdown` missing from `output_service.py` — out of scope). Remaining S31 bugs open: (3) feedback_service.py:24 `base_dir`→`feedback_dir`, plus source/binary divergence and two others per diag-S31.
- Tauri shell: STALE on disk (8.54MB, timestamp 2026-08-21 15:30:23) — not rebuilt this slice (NON-GOAL)
- Backend hash: freshly rebuilt with PyInstaller — deployed to D:\Scan2Text\backend\scan2text-backend.exe (43.51MB / 45,591,824 bytes)
- UPX: NOT installed on build machine — upx=False
- pdfium.dll: present in portable dist (_internal/pypdfium2_raw/pdfium.dll, 6.88MB)
- _internal: 707+ files, all DLLs present (python312.dll, llama.dll, pdfium.dll, 4 VC++ runtime DLLs)
- Backend tests: 339 passed + 0 failures. Full gate GREEN.
- Frontend tests: 666 passed, 0 failed. Full suite GREEN. Typecheck clean.
- Rust check: cargo check passed (dev profile compiled in 1.95s)
- version.json: deployed to D:\Scan2Text\version.json with CEO-verified hashes (vlm_sha256=9facc171..., mmproj_sha256=d63a90a1...)
- PRD: v1.12 source of truth in second-brain/04-Product/
- RESULT: All gates GREEN. Backend rebuilt and deployed to portable root. S28 zip extraction + S24 path_service fix + S25 kill fix all active.
- Tauri shell: STALE on disk (8.54MB, timestamp 2026-08-21 15:30:23) — not rebuilt this slice (NON-GOAL)
- Backend hash: freshly rebuilt with PyInstaller — deployed to D:\Scan2Text\backend\scan2text-backend.exe (43.51MB / 45,591,824 bytes)
- UPX: NOT installed on build machine — upx=False
- pdfium.dll: present in portable dist (_internal/pypdfium2_raw/pdfium.dll, 6.88MB)
- _internal: 707+ files, all DLLs present (python312.dll, llama.dll, pdfium.dll, 4 VC++ runtime DLLs)
- Backend tests: 339 passed + 0 failures. Full gate GREEN.
- Frontend tests: 666 passed, 0 failed. Full suite GREEN. Typecheck clean.
- Rust check: cargo check passed (dev profile compiled in 1.95s)
- version.json: deployed to D:\Scan2Text\version.json with CEO-verified hashes (vlm_sha256=9facc171..., mmproj_sha256=d63a90a1...)
- PRD: v1.12 source of truth in second-brain/04-Product/
- RESULT: All gates GREEN. Backend rebuilt and deployed to portable root. S28 zip extraction + S24 path_service fix + S25 kill fix all active.

## Recent Changelog
- **2026-08-23 (S32b-ENGINE-MODELNAME-WEBVIEW-REMOVAL):** FIX — **`engine.py`: removed `import webview`, deleted `headless=False` pywebview branch, simplified `launch_app()` to headless-only (dropped `headless` param), pinned model filename `ovisocr2-q8.gguf`→`vlm.gguf`.** 3 targeted tests added (`test_engine_webview_removal.py`): filename pin GREEN, webview-absence GREEN, import test RED by pre-existing `save_markdown` missing from `output_service.py` (out of scope). py_compile clean. Commit: `332cec8`.
- **2026-08-23 (S32a-FIX-PATHSERVICE-API):** FIX — **`get_paths()` accessor + `ensure_dirs` alias added to path_service.py.** TDD RED→GREEN: 4 new tests (TestGetPathsAccessor ×2, TestEnsureDirsAlias ×2) confirm `get_paths()` returns the `_default_instance` singleton and `ensure_dirs` is an alias of `PathService.ensure_runtime_dirs`. Frozen regression: 14/14 pass. Line 71 `parent.parent` confirmed correct for locked portable layout — NOT touched (misdiagnosis per diag-S31). Commit: `af917dc`.
- **2026-08-23 (S31-RECON-PROBE):** DIAG — **RECONCILED: 5 independent bugs identified, fix design finalized (READ-ONLY, zero edits).** (1) `path_service.py:71` S24 `parent.parent`→`parent` math error returns drive root instead of portable root. (2) Missing `get_paths()` accessor — engine.py line 49 ImportError. (3) Missing `ensure_dirs()` alias — engine.py line 51 AttributeError. (4) `engine.py:57` references `ovisocr2-q8.gguf` but disk has `vlm.gguf`. (5) `feedback_service.py:24` uses `base_dir` (=backend/) instead of `feedback_dir` property — feedback lands in backend/feedback/ not portable root/. Binary byte-scan confirms source/binary divergence (binary lacks all three strings). Full report: `second-brain/01-Agent-Memory/Phase-11/diag-S31-RECON-PROBE.md`.
- **2026-08-23 (S31-RECON-PROBE):** DIAG — **RECONCILED: 5 independent bugs identified, fix design finalized (READ-ONLY, zero edits).** (1) `path_service.py:71` S24 `parent.parent`→`parent` math error returns drive root instead of portable root. (2) Missing `get_paths()` accessor — engine.py line 49 ImportError. (3) Missing `ensure_dirs()` alias — engine.py line 51 AttributeError. (4) `engine.py:57` references `ovisocr2-q8.gguf` but disk has `vlm.gguf`. (5) `feedback_service.py:24` uses `base_dir` (=backend/) instead of `feedback_dir` property — feedback lands in backend/feedback/ not portable root/. Binary byte-scan confirms source/binary divergence (binary lacks all three strings). Full report: `second-brain/01-Agent-Memory/Phase-11/diag-S31-RECON-PROBE.md`.
- **2026-08-23 (S30-DIAG-MODELS-PATH-SPLIT):** DIAG — **ROOT CAUSE: `_resolve_app_root()` returns `D:\` instead of `D:\Scan2Text` (S24 math error).** S24 changed frozen branch from `_resolve_portable_root()` to `exe_dir.parent.parent`, but locked layout has `backend/` as a subdirectory of portable root, so `parent.parent` = drive root not portable root. `_resolve_portable_root()` correctly returns `D:\Scan2Text` (finds models/ at exe_dir.parent). Consumer split: OCREngine uses `_resolve_models_dir()` (correct by accident), FeedbackService uses `base_dir` (=exe_dir=backend/) so feedback lands in backend/feedback/, ModelDownloaderService pinned at import with broken app_root. Models exist at D:\Scan2Text\models\ (GGUF verified) but engine reports MODEL_NOT_FOUND — running binary has diverged code (references `get_paths()`/`exe_root` which don't exist in source). Fix design: change path_service.py:71 `parent.parent`→`parent`, fix FeedbackService to use `feedback_dir` prop, resolve source/binary divergence. Full report: `second-brain/01-Agent-Memory/Phase-11/diag-S30-MODELS-PATH-SPLIT.md`.
- **2026-08-23 (S28-BACKEND-ZIP-EXTRACTION):** FIX — **Downloader now streams `.zip` archives from GitHub Releases, verifies size + SHA256, extracts first `.gguf` entry, and cleans up.** `model_downloader_service.py`: added `zipfile` import; download flow changed to `.zip.part` → atomic rename → `.zip` → size/hash verify against `version.json` → extract `.gguf` → delete zip. Skip optimization relaxed to existence check (no gguf hash stored). Progress + cancel preserved. Tests: 17 passed (added `TestZipExtraction`). `version.json`: updated `vlm_sha256`, `mmproj_sha256` to CEO-verified hashes; updated `vlm_size_bytes` (772878446) and `mmproj_size_bytes` (156370919) from local zips at `D:\Scan2Text\models\`. Commit: `ca0c817`.
- **2026-08-23 (S27-DIAG-DOWNLOAD-404):** DIAG — **ROOT CAUSE: GitHub Release `OCR` absent + downloader design flaw**. Downloader modal appears (S24 verified) but backend log shows "Download failed for vlm.gguf: HTTP Error 404: Not Found". Root cause: (1) GitHub Release with tag `OCR` and assets `vlm.zip`/`mmproj.zip` does not exist or is not accessible — all three URL probes return 404; (2) Even if URLs resolved, downloader has critical design flaw: downloads ZIP archives but saves them as `.gguf` without extraction (`model_downloader_service.py:22-24,151` hardcodes `.gguf` output filenames; zero `zipfile` imports exist in codebase). Fix design: either publish GitHub Release assets OR add zipfile extraction logic to downloader. No source edits. Full report: `second-brain/01-Agent-Memory/Phase-11/diag-S27-DOWNLOAD-404.md`.
- **2026-08-23 (S25-FIX-RUST-KILL-IMAGE-NAME):** FIX — **`stop_backend()` now uses `taskkill /F /IM scan2text-backend.exe /T` instead of `/PID`.** Resolves KILL_SILENT_FAILURE (ZOMBIE_BACKEND) from S23. The Python backend daemonizes — outer PID dies immediately, so `/PID`-targeted kill fails silently and the inner Uvicorn daemon survives as a zombie (~65MB, holding port 47351). Fix: extracted `build_kill_command()` that builds the CEO-locked command (`/F /IM scan2text-backend.exe /T`), updated `stop_backend()` to use it, changed failure logging from `warn` to `error` with `eprintln!` fallback for release builds, and added TDD test `test_kill_command_uses_image_name_not_pid`. Only `frontend/src-tauri/src/backend_process.rs` changed.
- **2026-08-23 (S24-FIX-PATHSERVICE-PORTABLE-ROOT-ANCHOR):** FIX — **`_resolve_app_root()` now anchors on locked layout (`exe_dir.parent.parent`) instead of fragile `models/` presence.** S22 diagnosed MODELS_ANCHOR_FAILS_WHEN_ABSENT: `_resolve_portable_root()` falls back to `exe_dir` when no `models/` ancestor is found, causing version.json lookups at the wrong path. Fix: changed frozen branch in `_resolve_app_root()` from `return PathService._resolve_portable_root()` to `return Path(sys.executable).parent.parent`. Updated existing test assertion and added `test_frozen_app_root_resolves_to_portable_root_without_models_dir` (no models/ dir created). 14/14 frozen tests pass. Commit: `c981dad`. (NOTE from S30: this fix is mathematically incorrect — `parent.parent` = drive root, should be `parent`. See diag-S30.)
