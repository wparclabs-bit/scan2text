# Scan2Text Current State

<!-- MAINTENANCE PROTOCOL: Keep only the Baseline block + last 5 changelog entries here. When you add a new entry, move the oldest entry to second-brain/01-Agent-Memory/Archive/state-history.md. This protects the 45k token cap (AGENTS.md 3.2). -->

## Baseline
- Phase: Phase 11 (Gate + Tauri Rebuild) — S11-GATE-TAURI-REBUILD-DEPLOY
- Date: 2026-08-20
- Tauri shell hash: 6B56B7310BAF98AC10753AAFCCAC8A5ED287C016468E5D6A227D3F2BD66622FE (supersedes 9E49C4971A2C939E77045A54773DC8FC6702CBAA10DDA5321AAA740872656B4B)
- Backend hash: B94612C9F074D4EA734D7090FA7A63050E409C9405AC95E1AC4CF810409F2A53
- UPX: NOT installed on build machine — upx=False
- pdfium.dll: present in portable dist (_internal/pypdfium2_raw/pdfium.dll, 6.88MB)
- _internal: 765 files, all DLLs present (python312.dll, llama.dll, pdfium.dll, 4 VC++ runtime DLLs)
- Backend tests: 330 passed, 2 failed (332 total) — test_health_contract (pre-existing) + test_all_enum_values_present (NEW: FIX73 added PARTIAL_FAILURE to ErrorCode enum without updating this membership test); GATE3 BLOCKED, see changelog
- Frontend tests: 647 passed, 0 failures (new baseline — 37 test files)
- PRD: v1.12 source of truth in second-brain/04-Product/
- RESULT: S11-GATE2-TAURI-REBUILD-DEPLOY COMPLETE. Full frontend gate (647/647 new baseline, typecheck clean — 37 test files). Tauri rebuild: Vite build + Rust release compile (31.9s) + NSIS/MSI bundles. Single-file exe swap to D:\Scan2Text\Scan2Text.exe (SHA256 6B56B7310BAF98AC10753AAFCCAC8A5ED287C016468E5D6A227D3F2BD66622FE, destination hash verified). Boot proof: NO console window, /api/health at 15s → status ok, OvisOCR2 0.9B loaded (model.loaded true), worker idle, version 0.1.0. 9E49C497 shell superseded. Zero source edits. S11-FIX73-STATUS-SEMANTICS COMPLETE — backend status semantics fixed via TDD.

## Recent Changelog
- **2026-08-20 (S11-GATE3-BACKEND-REBUILD-DEPLOY):** BLOCKED — Full backend gate run: 330 passed, 2 failed (332 total = 322 baseline + 4 FIX73 + 5 FIX74, count matches). Failures: (1) `test_health_contract` — PRE-EXISTING, expected. (2) `tests/unit/test_error_mapping.py::TestErrorMapping::test_all_enum_values_present` — NEW failure NOT in baseline. ROOT CAUSE: FIX73 (844a2e6) added `PARTIAL_FAILURE` to the `ErrorCode` enum (`src/scan2text/models/errors.py:19`) but never updated this exhaustive-membership test, which still asserts the old expected set without `PARTIAL_FAILURE`, so `expected == actual` fails on the extra member. `test_error_mapping.py` last touched by FIX59 (1d491dc), untouched by FIX73/74. Per slice rule ("ANY unexpected failure besides pre-existing test_health_contract = BLOCK; do not rebuild") → did NOT rebuild/deploy. Backend binary at D:\Scan2Text\backend STILL STALE (B94612C9…). ONE-LINE FIX for next slice: add `"PARTIAL_FAILURE"` to the `expected` set in `test_all_enum_values_present`, then re-run gate. Zero source edits made this slice.
- **2026-08-20 (S11-FIX74-PDF-PAGE-RESILIENCE):** COMPLETE — Per-page PDF resilience in `src/scan2text/adapters/vlm_ocr.py:272-299` (`VlmOcrAdapter.ocr()` per-page loop): wrapped the per-page `extract_and_save_image_crops` call in try/except; failed pages skipped with a privacy-safe log entry (page index + OCR_FAILED code only — no filename/content, NFR-02); successful pages collected IN ORDER and joined into ONE .md with FR-06 separators; PDF returns OCR_FAILED dict (→ quarantine, no .md) only when ALL pages fail. Mirrors FIX73 page-level: any page succeeds → completed/PARTIAL_FAILURE (logged only); zero succeed → failed. New tests: `tests/test_vlm_ocr_pdf_page_resilience.py` (5 tests). Targeted regression: 67 passed across vlm_ocr/PDF/queue/status paths. Full suite deferred to GATE3. Deployed backend binary at D:\Scan2Text\backend STILL STALE (B94612C9…) — rebuild/deploy is GATE3, NOT this slice.
- **2026-08-20 (S11-FIX73-STATUS-SEMANTICS):** COMPLETE — Backend status semantics fixed (TDD RED→GREEN). `src/scan2text/api/main.py:123-127` — changed `if summary.failed > 0` → `if succeeded == 0 AND failed > 0 → failed; elif succeeded > 0 AND failed > 0 → completed with error_code=PARTIAL_FAILURE`. Added `PARTIAL_FAILURE` to `ErrorCode` enum in `src/scan2text/models/errors.py`. Tests: 4 new in `tests/test_status_semantics.py` + 46 targeted api/queue tests all passing. CEO-approved Option A: partial batches show green/locked FR-04 "completed" status. Defect 2 (PDF per-page atomicity) deferred to FIX74. Full suite deferred to GATE slice.
- **2026-08-20 (S11-DIAG-BACKEND-STATUS-SEMANTICS):** DIAG — ROOT CAUSE ISOLATED. Two defects: (1) `src/scan2text/api/main.py:123` — `summary.failed > 0` marks ENTIRE task failed when ANY file in batch fails, producing false-red for successful files (JnT.jpeg .md exists & is good but shows red). (2) PDF processing atomic per-file — per-page timeout or post-processing error fails the whole PDF with no .md written. Fix plan: introduce `partial_failure` status; make PDF post-processing page-resilient. Test to go RED first: `test_status_semantics.py`. Logs contain no Python application error entries (logging infrastructure gap). Zero source edits.
- **2026-08-20 (S11-GATE2-TAURI-REBUILD-DEPLOY):** COMPLETE — Full frontend gate: 647/647 tests (37 files, new baseline), typecheck clean. Tauri rebuild via npx @tauri-apps/cli build: Vite build + Rust release compile (31.9s) + NSIS/MSI bundles. New Scan2Text.exe SHA256 6B56B7310BAF98AC10753AAFCCAC8A5ED287C016468E5D6A227D3F2BD66622FE. Single-file exe swap to D:\Scan2Text\Scan2Text.exe (destination hash verified). Boot proof: NO console window, /api/health at 15s → status ok, OvisOCR2 0.9B loaded (model.loaded true), worker idle, version 0.1.0. 9E49C497 superseded. Zero source edits.
- **2026-08-20 (S11-FIX72-HEALTH-RETRY-RESILIENCE):** COMPLETE — Background health loop no longer fails a job on a single `getHealth()` rejection. Added `consecutiveHealthFailures` counter to `ScanJob`; only fails after 3 consecutive health probe failures, resets to 0 on any successful probe. TDD: RED→GREEN with 3 new/modified tests (97 total store tests, all passing). Typecheck clean. QueuePanel.integration.test.tsx updated for new `ScanJob` field.
