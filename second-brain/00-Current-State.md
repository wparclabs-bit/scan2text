# Scan2Text Current State

<!-- MAINTENANCE PROTOCOL: Keep only the Baseline block + last 5 changelog entries here. When you add a new entry, move the oldest entry to second-brain/01-Agent-Memory/Archive/state-history.md. This protects the 45k token cap (AGENTS.md 3.2). -->

## Baseline
- Phase: S28-BACKEND-ZIP-EXTRACTION (COMPLETE)
- Date: 2026-08-23
- Status: COMPLETE — zip download + extraction implemented; version.json updated with CEO-verified hashes
- Tauri shell hash: (rebuilt with S25 kill fix) — timestamp 2026-08-21 15:30:23
- Backend hash: (rebuilt with S24 path_service fix + S25 kill fix) — timestamp 2026-08-21 15:28:28
- UPX: NOT installed on build machine — upx=False
- pdfium.dll: present in portable dist (_internal/pypdfium2_raw/pdfium.dll, 6.88MB)
- _internal: 707+ files, all DLLs present (python312.dll, llama.dll, pdfium.dll, 4 VC++ runtime DLLs)
- Backend tests: 339 passed + 0 failures. Full gate GREEN. (S28 added 1 new test)
- Frontend tests: 666 passed, 0 failed. Full suite GREEN. Typecheck clean. Build success.
- Rust check: cargo check passed (release profile compiled in 29.57s)
- PRD: v1.12 source of truth in second-brain/04-Product/
- RESULT: S26-GATE-FINAL-REBUILD-DEPLOY COMPLETE. All gates GREEN. PyInstaller rebuild + Tauri release build successful. Deployed to D:\Scan2Text\: Scan2Text.exe (8.54MB), backend\scan2text-backend.exe (43.51MB), version.json at portable root. S24 (_resolve_app_root() fix) and S25 (taskkill /IM kill fix) now active in deployed binary.

## Recent Changelog
- **2026-08-23 (S28-BACKEND-ZIP-EXTRACTION):** FIX — **Downloader now streams `.zip` archives from GitHub Releases, verifies size + SHA256, extracts first `.gguf` entry, and cleans up.** `model_downloader_service.py`: added `zipfile` import; download flow changed to `.zip.part` → atomic rename → `.zip` → size/hash verify against `version.json` → extract `.gguf` → delete zip. Skip optimization relaxed to existence check (no gguf hash stored). Progress + cancel preserved. Tests: 17 passed (added `TestZipExtraction`). `version.json`: updated `vlm_sha256`, `mmproj_sha256` to CEO-verified hashes; updated `vlm_size_bytes` (772878446) and `mmproj_size_bytes` (156370919) from local zips at `D:\Scan2Text\models\`. Commit: `ca0c817`.
- **2026-08-23 (S27-DIAG-DOWNLOAD-404):** DIAG — **ROOT CAUSE: GitHub Release `OCR` absent + downloader design flaw**. Downloader modal appears (S24 verified) but backend log shows "Download failed for vlm.gguf: HTTP Error 404: Not Found". Root cause: (1) GitHub Release with tag `OCR` and assets `vlm.zip`/`mmproj.zip` does not exist or is not accessible — all three URL probes return 404; (2) Even if URLs resolved, downloader has critical design flaw: downloads ZIP archives but saves them as `.gguf` without extraction (`model_downloader_service.py:22-24,151` hardcodes `.gguf` output filenames; zero `zipfile` imports exist in codebase). Fix design: either publish GitHub Release assets OR add zipfile extraction logic to downloader. No source edits. Full report: `second-brain/01-Agent-Memory/Phase-11/diag-S27-DOWNLOAD-404.md`.
- **2026-08-23 (S25-FIX-RUST-KILL-IMAGE-NAME):** FIX — **`stop_backend()` now uses `taskkill /F /IM scan2text-backend.exe /T` instead of `/PID`.** Resolves KILL_SILENT_FAILURE (ZOMBIE_BACKEND) from S23. The Python backend daemonizes — outer PID dies immediately, so `/PID`-targeted kill fails silently and the inner Uvicorn daemon survives as a zombie (~65MB, holding port 47351). Fix: extracted `build_kill_command()` that builds the CEO-locked command (`/F /IM scan2text-backend.exe /T`), updated `stop_backend()` to use it, changed failure logging from `warn` to `error` with `eprintln!` fallback for release builds, and added TDD test `test_kill_command_uses_image_name_not_pid`. Only `frontend/src-tauri/src/backend_process.rs` changed.
- **2026-08-23 (S24-FIX-PATHSERVICE-PORTABLE-ROOT-ANCHOR):** FIX — **`_resolve_app_root()` now anchors on locked layout (`exe_dir.parent.parent`) instead of fragile `models/` presence.** S22 diagnosed MODELS_ANCHOR_FAILS_WHEN_ABSENT: `_resolve_portable_root()` falls back to `exe_dir` when no `models/` ancestor is found, causing version.json lookups at the wrong path. Fix: changed frozen branch in `_resolve_app_root()` from `return PathService._resolve_portable_root()` to `return Path(sys.executable).parent.parent`. Updated existing test assertion and added `test_frozen_app_root_resolves_to_portable_root_without_models_dir` (no models/ dir created). 14/14 frozen tests pass. Commit: `c981dad`.
- **2026-08-23 (S23-DIAG-ZOMBIE-BACKEND-KILL-TREE):** DIAG — **ROOT CAUSE: KILL_SILENT_FAILURE via backend daemonization + PID-targeted kill.** Closing the Tauri shell leaves `scan2text-backend.exe` alive (~65MB, holding port 47351), violating NFR-05 / FIX77. Forensics: spawn wiring correct (`BackendManager.start()` → `start_backend()`, `self.child = Some(child)` at backend_process.rs:66); kill wiring correct and present in the deployed binary — `RunEvent::WindowEvent::CloseRequested` (lib.rs:398) → `guard.stop()` (lib.rs:402) → `stop_backend()` (backend_process.rs:285) → `taskkill /F /T /PID <pid>`. Deployed shell hash MATCHES release target (`8CDF8DD6…`), so NOT a stale deploy. Live probe (WM_CLOSE) reproduced the zombie: shell dies, inner daemon survives, port held. **New mechanism found:** the Python backend *daemonizes* — the process Rust spawns (outer) spawns a second `scan2text-backend.exe` (inner Uvicorn daemon that holds 47351) and exits within ~2s. Rust tracks the outer PID as `self.child`; on close `taskkill /F /T /PID <outer_pid>` reports "process not found" (outer already gone), the `log::warn!` is invisible in release (no logger installed — plugin only added under `cfg(debug_assertions)`, lib.rs:367), and the native `child.kill()` fallback no-ops on the dead child → inner daemon survives silently. Kill command uses `/PID` not the CEO-locked `/IM scan2text-backend.exe`, so it cannot catch the daemonized inner. Recommended remediation (NOT implemented): kill by image name per locked intent (`taskkill /F /IM scan2text-backend.exe /T`) or track the inner daemon's PID from its PID file; add a release logger so kill-path failures surface. Full report: `second-brain/01-Agent-Memory/Phase-11/diag-S23-ZOMBIE-BACKEND-KILL-TREE.md`.
