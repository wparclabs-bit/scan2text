# Scan2Text Current State

<!-- MAINTENANCE PROTOCOL: Keep only the Baseline block + last 5 changelog entries here. When you add a new entry, move the oldest entry to second-brain/01-Agent-Memory/Archive/state-history.md. This protects the 45k token cap (AGENTS.md 3.2). -->

## Baseline
- Phase: GATE3-RETRY (Test-Drift Remediation + Backend Rebuild/Deploy) — S11-GATE3-RETRY
- Date: 2026-08-20
- Tauri shell hash: 6B56B7310BAF98AC10753AAFCCAC8A5ED287C016468E5D6A227D3F2BD66622FE (supersedes 9E49C4971A2C939E77045A54773DC8FC6702CBAA10DDA5321AAA740872656B4B)
- Backend hash: DAEEFBCCC0A9084B3C4B05BE59628FC76BECFF50E462BC470947FA0CA437DC8D (supersedes B94612C9F074D4EA734D7090FA7A63050E409C9405AC95E1AC4CF810409F2A53 — GATE3-RETRY rebuild)
- UPX: NOT installed on build machine — upx=False
- pdfium.dll: present in portable dist (_internal/pypdfium2_raw/pdfium.dll, 6.88MB)
- _internal: 707 files (actual GATE3-RETRY build), all DLLs present (python312.dll, llama.dll, pdfium.dll, 4 VC++ runtime DLLs)
- Backend tests: 331 passed + 1 pre-existing failure (test_health_contract). test_all_enum_values_present FIXED in GATE3-RETRY (PARTIAL_FAILURE added to expected set; FIX73's ErrorCode member now covered). Full gate GREEN except pre-existing.
- Frontend tests: 647 passed, 0 failures (new baseline — 37 test files)
- PRD: v1.12 source of truth in second-brain/04-Product/
- RESULT: S11-GATE3-RETRY COMPLETE. One-line test-drift fix (added "PARTIAL_FAILURE" to expected set in test_all_enum_values_present; FIX73's ErrorCode member now covered). Full backend gate: 331 passed + 1 pre-existing failure (test_health_contract ONLY) — matches expected. PyInstaller build via packaging/scan2text-backend.spec (--clean -y), exit 0, dist=packaging/dist/scan2text-backend (707 files, _internal/pypdfium2_raw/pdfium.dll present). Deployed to D:\Scan2Text\backend via $dist\* wildcard. NEW SHA256 DAEEFBCCC0A9084B3C4B05BE59628FC76BECFF50E462BC470947FA0CA437DC8D (≠ stale B94612C9…). Boot proof @127.0.0.1:47351/api/health → status ok, OvisOCR2 0.9B loaded (model.loaded true), worker idle, version 0.1.0. Boot instance killed; port 47351 free. Zero source edits (test-only). Tauri shell untouched (6B56B731…).

## Recent Changelog
- **2026-08-20 (S11-FIX76-RUST-ORPHAN-KILL):** COMPLETE — Replaced `frontend/src-tauri/src/backend_process.rs:283-287` `stop_backend()` with Windows `taskkill /F /T /PID <pid>` to kill entire Python multiprocessing process tree. Added CREATE_NO_WINDOW flag (0x08000000) to suppress console flash. Fallback: native `child.kill()` always attempted as last resort. Port-wait loop: blocks until 47351 released (up to 5s). Non-Windows: graceful fallback with warning log. cargo check: zero errors, zero warnings. Commit `6a1f07f`.
- **2026-08-20 (S11-FIX75-HEALTH404-TYPO):** COMPLETE — TDD RED→GREEN. Root cause: `frontend/src/lib/api.ts:116` called `fetch(buildApiUrl('/health'))` — missing `/api/` prefix. Backend only serves `/api/health`. Background re-poll loop (store.ts:507) calls `getHealth()` every 60s; each 404 incremented `consecutiveHealthFailures`; after 3 failures (FIX72 threshold), job falsely marked failed with `errors.backendLost` toast. Fix: one-line edit `/health` → `/api/health`. Test gap closure: added `getHealth()` test in `api.test.ts` asserting canonical `/api/health` path via mocked fetch — this test was RED before the fix, confirming the bug was undetectable by prior gates. Frontend: 647 passed, 0 failures. Typecheck clean. Zero backend/Rust changes.
- **2026-08-20 (S11-DIAG-LINGER-PROCESSES):** DIAG — CONFIRMED. Root causes: (1) **WORKER-CHILD**: Second backend process is a Python multiprocessing fork (`--multiprocessing-fork`, parent_pid=8020) spawned by the first backend — NOT a shell double-spawn. Live evidence: PID 15380 parented by PID 8020, created 2s after first backend. (2) **ORPHAN-ON-EXIT**: Both backends survive after shell kill. Live evidence: killed Scan2Text.exe (PID 20400), both PIDs 8020+15380 survived. Root cause: backend's parent PID is 9548 (phantom/dead intermediary process), NOT the Rust shell (20400). Rust shell's BackendManager.child handle disconnected from actual backend process tree. Cleanup handlers fire on a stale handle. Minimal fix: kill process tree on exit (`taskkill /F /T /PID`), capture actual PID via port query, add spawn guard. Zero source edits. Full report: `second-brain/01-Agent-Memory/Phase-11/slice-S11-DIAG-LINGER-PROCESSES.md`.
- **2026-08-20 (S11-FIX74-PDF-PAGE-RESILIENCE):** COMPLETE — Per-page PDF resilience in `src/scan2text/adapters/vlm_ocr.py:272-299` (`VlmOcrAdapter.ocr()` per-page loop): wrapped the per-page `extract_and_save_image_crops` call in try/except; failed pages skipped with a privacy-safe log entry (page index + OCR_FAILED code only — no filename/content, NFR-02); successful pages collected IN ORDER and joined into ONE .md with FR-06 separators; PDF returns OCR_FAILED dict (→ quarantine, no .md) only when ALL pages fail. Mirrors FIX73 page-level: any page succeeds → completed/PARTIAL_FAILURE (logged only); zero succeed → failed. New tests: `tests/test_vlm_ocr_pdf_page_resilience.py` (5 tests). Targeted regression: 67 passed across vlm_ocr/PDF/queue/status paths. Full suite deferred to GATE3. Deployed backend binary at D:\Scan2Text\backend STILL STALE (B94612C9…) — rebuild/deploy is GATE3, NOT this slice.
- **2026-08-20 (S11-FIX73-STATUS-SEMANTICS):** COMPLETE — Backend status semantics fixed (TDD RED→GREEN). `src/scan2text/api/main.py:123-127` — changed `if summary.failed > 0` → `if succeeded == 0 AND failed > 0 → failed; elif succeeded > 0 AND failed > 0 → completed with error_code=PARTIAL_FAILURE`. Added `PARTIAL_FAILURE` to `ErrorCode` enum in `src/scan2text/models/errors.py`. Tests: 4 new in `tests/test_status_semantics.py` + 46 targeted api/queue tests all passing. CEO-approved Option A: partial batches show green/locked FR-04 "completed" status. Defect 2 (PDF per-page atomicity) deferred to FIX74. Full suite deferred to GATE slice.

